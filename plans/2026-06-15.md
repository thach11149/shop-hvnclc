# Kế hoạch ngày 2026-06-15

**Agent:** 1 agent chính (có thể tách 2-3 agent song song theo domain)
**Branch:** tạo từ `origin/claude/elegant-dijkstra-f8na2g` (branch Agent 3 mới nhất, chứa code cả 3 agent hôm qua)
**Nguyên tắc:** làm thật, không placeholder. Xong nhóm chính → tự lấy nhóm dự phòng làm tiếp.

---

## Trạng thái đầu ngày

3 agent hôm qua đã xong:
- Agent 1: buyer-web (Home/Products/Category/Account), seller (Finance/Reports/EditProduct), admin (Dashboard/Orders/Sellers/Users)
- Agent 2: seller (Campaigns/ShopPage/Affiliate/Ads/ReturnRequests/AIPricePage/AIInventory), buyer (Cart/Orders/Wishlist/Loyalty/Checkout)
- Agent 3: admin (Returns/Withdrawals/Promotions/Banners/Disputes/ProductsPage/ShippingCarriers/FraudCases/Marketing), buyer (Referral/DisputeDetail)

**Còn lại chưa nâng cấp (thin pages 100-170 dòng):**
- buyer-web: ShopPage (169), OrderDetailPage (271 — cần thêm features)
- seller-center: ProductsPage (113), OrderDetailPage (113), WarehousePage (128), WarehouseInboundPage (118), DisputesPage (119), FlashSalePage (171), NewProductPage (117)
- admin-console: SellerDetailPage (199), FlashSaleAdminPage (166), CampaignsPage (173), BIDashboardPage (117), AffiliateManagementPage (187)

---

## NHÓM CHÍNH — Bắt buộc hoàn thành

### Task 1 — buyer-web ShopPage nâng cấp thật

Trang hiện tại 169 dòng. Không có agent nào làm. Làm thật:

**Shop header:**
- Ảnh bìa full-width (banner), overlay gradient nhẹ
- Avatar shop hình tròn + tên shop + verified badge (nếu có)
- Stats bar: ★ rating trung bình, số đánh giá, X người theo dõi, X sản phẩm, X đơn hoàn thành
- **Follow/Unfollow button** (gọi `POST /users/follow-shop` → cập nhật count ngay lập tức)
- Nút "Nhắn tin" → mở chat với seller

**Tab bar (3 tabs):**
1. **Sản phẩm**: grid sản phẩm của shop
   - Search trong shop (input)
   - Filter theo category (tabs từ shop categories)
   - Sort: Bán chạy | Mới nhất | Giá tăng/giảm
   - Pagination
   - Gọi `GET /shops/:slug/products?search=&category=&sort=&page=`
2. **Đánh giá**: aggregate reviews
   - Rating overview (5 sao → 1 sao với %)
   - List review cards: avatar, tên (ẩn bớt), sao, comment, tên sản phẩm, ngày
   - Gọi `GET /shops/:slug/reviews?page=`
3. **Thông tin**: shop details
   - Mô tả shop, địa chỉ kho hàng, giờ làm việc
   - Chính sách hoàn trả
   - Ngày tham gia, tỷ lệ phản hồi

**Backend cần kiểm tra/bổ sung (seller.routes.ts):**
- `GET /shops/:slug` — trả về shop info với stats (followers, rating, productCount)
- `GET /shops/:slug/products?search=&category=&sort=&page=`
- `GET /shops/:slug/reviews?page=`

---

### Task 2 — seller-center ProductsPage + OrderDetailPage nâng cấp

**ProductsPage** (hiện 113 dòng → cần ~380 dòng):
- **Filter bar:** search tên/SKU, status tabs (Tất cả | Đang bán | Nháp | Chờ duyệt | Bị từ chối | Hết hàng), filter theo category
- **Table columns:** ảnh thumbnail, tên sản phẩm, SKU, giá, tồn kho, trạng thái, số đơn, ngày tạo
- **Stock indicator:** màu đỏ nếu tồn kho < 5, vàng nếu < 20
- **Bulk actions:** checkbox → Kích hoạt / Tạm dừng / Xóa (confirm)
- **Row actions:** Edit, Copy (duplicate product), Quản lý variants, Xem trang sản phẩm
- Pagination + export CSV

**OrderDetailPage seller** (hiện 113 dòng → cần ~350 dòng):
- **Order header:** mã đơn, trạng thái badge, ngày đặt, tổng tiền
- **Timeline trạng thái:** step-by-step (Đặt hàng → Xác nhận → Đóng gói → Giao vận → Hoàn thành) với timestamp từng bước
- **Thông tin người mua:** tên, SĐT, địa chỉ giao hàng
- **Danh sách sản phẩm:** ảnh, tên, variant, số lượng, đơn giá, thành tiền
- **Thông tin vận chuyển:** đơn vị vận chuyển, mã tracking, nút "Cập nhật tracking"
- **Actions phù hợp trạng thái:** Xác nhận đơn / Tạo phiếu giao / In đơn hàng / Hoàn thành

**Backend bổ sung (seller.routes.ts / order.routes.ts):**
- `GET /seller/products?status=&category=&search=&page=` — đảm bảo support đầy đủ filter
- `POST /seller/products/:id/duplicate` — clone sản phẩm

---

### Task 3 — seller-center WarehousePage + WarehouseInboundPage nâng cấp

**WarehousePage** (hiện 128 dòng → cần ~380 dòng):
- **KPI cards:** Tổng SKU, Sắp hết hàng (< 10), Hết hàng, Tổng giá trị tồn kho
- **Inventory table:**
  - Cột: ảnh, tên sản phẩm, SKU, tồn kho hiện tại (color-coded), ngưỡng cảnh báo, giá trị, trạng thái
  - Tồn kho < ngưỡng → badge đỏ "Sắp hết"
  - Filter: tất cả/sắp hết/hết hàng/đủ hàng
  - Search theo tên/SKU
- **Quick adjust:** click vào số tồn kho → inline input để điều chỉnh nhanh (+/-)
- **Export tồn kho** → CSV
- Link tới WarehouseInboundPage để nhập hàng

**WarehouseInboundPage** (hiện 118 dòng → cần ~350 dòng):
- **Form nhập hàng:**
  - Nhà cung cấp (input text hoặc select from saved suppliers)
  - Ngày dự kiến nhập
  - Thêm sản phẩm: search → chọn → nhập số lượng nhận + giá nhập
  - Danh sách sản phẩm đã thêm với tổng giá trị
  - Ghi chú
- **Trạng thái inbound:** Nháp | Đang chờ | Đã nhận một phần | Đã nhận đủ
- **Lịch sử inbound:** table (ngày, nhà CC, số sản phẩm, tổng giá trị, trạng thái, người tạo)
- Nút "Xác nhận đã nhận" → update stock ngay

**Backend cần bổ sung (warehouse.routes.ts):**
- `GET /warehouse/inventory?status=&search=&page=` — với low-stock filter
- `PATCH /warehouse/inventory/:productId` — điều chỉnh tồn kho
- `GET /warehouse/inbound` — list inbound orders
- `POST /warehouse/inbound` — tạo inbound mới
- `PATCH /warehouse/inbound/:id/receive` — xác nhận nhận hàng

---

### Task 4 — seller-center FlashSalePage + DisputesPage nâng cấp

**FlashSalePage** (hiện 171 dòng → cần ~400 dòng):
- **Danh sách flash sale của seller:**
  - Tabs: Sắp diễn ra | Đang chạy | Đã kết thúc
  - Countdown timer cho flash sale đang chạy
  - Mỗi event: tên, thời gian, số sản phẩm, trạng thái, actions (Edit/Xóa)
- **Create/Edit flash sale:**
  - Tên event, thời gian bắt đầu + kết thúc (datetime picker)
  - Thêm sản phẩm: search → chọn → nhập giá flash + số lượng giới hạn
  - Giá flash tự tính % giảm so giá gốc
  - Danh sách sản phẩm đã thêm
  - Submit gửi admin duyệt hoặc publish ngay

**DisputesPage seller** (hiện 119 dòng → cần ~370 dòng):
- Filter: trạng thái (open/responding/escalated/resolved), date range
- Mỗi dispute card: mã đơn, người mua, lý do, trạng thái, ngày mở
- **Detail panel (click vào):**
  - Thông tin đơn hàng
  - Timeline: buyer mở → seller phản hồi → admin review
  - Ảnh bằng chứng của buyer
  - **Form phản hồi seller:** textarea + upload ảnh counter-evidence
  - Nút "Gửi phản hồi" → `POST /disputes/:id/response`

**Backend cần bổ sung:**
- `GET /seller/flash-sale` — list flash sales của seller
- `POST /seller/flash-sale` — tạo flash sale
- `PATCH /seller/flash-sale/:id` — cập nhật
- `GET /seller/disputes?status=&page=` — disputes liên quan seller
- `POST /disputes/:id/response` — seller submit phản hồi

---

### Task 5 — admin-console SellerDetailPage nâng cấp

Trang hiện tại 199 dòng. Quan trọng nhất trong admin. Làm thật:

**Header:**
- Banner shop + avatar + tên + verified badge
- Nhanh actions: **Duyệt** / **Tạm khóa** / **Nâng tier** (Bronze→Silver→Gold) / **Export data**

**Tab 1 — Tổng quan:**
- KPI cards: GMV tháng này, Số đơn, Tỷ lệ hoàn thành, Rating trung bình, Số sản phẩm, Số người theo dõi
- Mini revenue chart 30 ngày (recharts AreaChart nhỏ)
- Thông tin đăng ký: email, SĐT, địa chỉ kho, ngày tham gia, tier hiện tại

**Tab 2 — Sản phẩm (top 20):**
- Table: ảnh, tên, giá, tồn kho, số đơn, trạng thái
- Nút Duyệt/Từ chối nhanh cho từng sản phẩm

**Tab 3 — Đơn hàng (20 gần nhất):**
- Table: mã đơn, người mua, tổng tiền, trạng thái, ngày
- Link tới chi tiết đơn

**Tab 4 — Tranh chấp & vi phạm:**
- List disputes liên quan seller
- Số vi phạm chính sách (nếu có fraud flags)

**Backend cần bổ sung (admin.service.ts / admin.routes.ts — CUỐI FILE):**
- `GET /admin/sellers/:id/detail` — trả về full seller data với stats (GMV, orders, products, disputes)
- `GET /admin/sellers/:id/revenue-chart` — 30 ngày revenue

---

### Task 6 — admin-console FlashSaleAdminPage + CampaignsPage nâng cấp

**FlashSaleAdminPage** (hiện 166 dòng → cần ~400 dòng):
- **List flash sales:**
  - Tabs: Sắp diễn ra | Đang chạy | Đã kết thúc | Nháp
  - Countdown timer cho event đang chạy
  - Mỗi event: tên, thời gian, số sản phẩm, số seller tham gia, trạng thái

- **Create/Edit flash sale:**
  - Tên, thời gian (datetime picker)
  - Tab "Thêm sản phẩm": search sản phẩm từ catalog, set flash price + qty limit
  - Tab "Mời seller": search seller + invite hoặc tự add sản phẩm từ seller
  - Publish / Lưu nháp

**CampaignsPage** (hiện 173 dòng → cần ~380 dòng):
- Filter: loại (flash sale / voucher / freeship / bundle), trạng thái, date range
- Table: tên, loại, thời gian, participants, số lượt dùng/tối đa, trạng thái
- Create/edit modal đầy đủ
- Duplicate campaign
- Toggle active/inactive, delete với confirm

---

## NHÓM DỰ PHÒNG — Làm tiếp nếu còn token

### Task 7 — buyer-web OrderDetailPage nâng cấp

Trang hiện tại 271 dòng. Thêm features:
- **Tracking timeline** có tên đơn vị vận chuyển + mã tracking số + estimated delivery date
- **Per-item review button:** nút "Đánh giá" riêng từng sản phẩm → link tới trang đánh giá
- **Re-order button:** "Mua lại" → thêm tất cả sản phẩm vào giỏ hàng
- **Dispute status banner:** nếu đơn có dispute đang mở → hiện thông báo + link tới DisputeDetailPage
- **Hủy đơn** (chỉ khi pending): modal chọn lý do

### Task 8 — seller-center NewProductPage nâng cấp

Trang hiện tại 117 dòng, skeleton. Làm thật tương tự EditProductPage (đã có):
- Multi-image upload (tối đa 8 ảnh, drag reorder) — dùng lại component từ EditProductPage
- Description với markdown preview
- Variants matrix (size × color)
- Shipping info
- **Preview** → mở tab xem trang sản phẩm
- **Lưu nháp** / **Đăng bán** buttons

### Task 9 — admin-console BIDashboardPage + AffiliateManagementPage nâng cấp

**BIDashboardPage** (hiện 117 dòng):
- Revenue by category (recharts PieChart với legend)
- Revenue by region/province (recharts BarChart top 10 tỉnh)
- Conversion funnel: Visits → Add to cart → Checkout → Purchase
- Daily active users chart (recharts LineChart)
- Gọi `GET /admin/analytics/bi-summary`

**AffiliateManagementPage** (hiện 187 dòng):
- List affiliators: tên, số link, tổng click, tổng đơn, hoa hồng earned, hoa hồng pending
- Filter: trạng thái (active/pending payout), date range
- Approve payout hàng loạt
- Xem detail modal: danh sách đơn qua affiliate, links created

---

## Phân công nếu tách thành 2 agent song song

### Agent A — Seller-center + Buyer (Tasks 1-4)
Files: seller-center/ProductsPage, OrderDetailPage, WarehousePage, WarehouseInboundPage, FlashSalePage, DisputesPage + buyer-web/ShopPage
Backend: seller.routes/service, warehouse.routes/service

### Agent B — Admin-console (Tasks 5-6)
Files: admin-console/SellerDetailPage, FlashSaleAdminPage, CampaignsPage
Backend: admin.routes/service (CUỐI FILE)

**Các files không được sửa chung:**
- `backend/src/app.ts` — không cần đăng ký module mới
- `frontend/*/src/App.tsx` — không cần route mới

---

## Lưu ý

- Branch từ `origin/claude/elegant-dijkstra-f8na2g` (Agent 3's branch, mới nhất)
- Tất cả pages đã có route trong App.tsx — không cần thêm
- Tất cả modules đã đăng ký trong app.ts — không cần thêm
- Commit sau mỗi task, cập nhật PROGRESS.md
