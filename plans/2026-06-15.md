# Kế hoạch ngày 2026-06-15

**Agent:** 1 agent, toàn bộ backend + frontend
**Branch:** tạo từ `origin/main` mới nhất (không có WIP branch)
**Nguyên tắc:** làm thật, không placeholder. Xong nhóm chính → tự lấy nhóm dự phòng làm tiếp.

---

## Trạng thái đầu ngày

Đã xong 8 tasks ngày 14/6 (sidebar nav, ProductDetail, CategoryTree, SellerDashboard real-time, OrdersPage bulk, SearchPage filters, Chat upgrade, Admin Reports).

**Vấn đề còn lại:** Nhiều trang đang là **skeleton mỏng** (60-100 dòng), chỉ có layout nhưng không có logic thật. Ưu tiên: nâng cấp các trang quan trọng nhất từ skeleton thành functional.

---

## NHÓM CHÍNH — Bắt buộc hoàn thành

### Task 1 — HomePage buyer-web nâng cấp thật

Trang hiện tại 134 dòng, chỉ có layout tĩnh. Làm thật:

**Banner carousel:**
- Slider tự động chạy mỗi 4 giây, có indicators và nút prev/next
- Load banners từ `GET /banners` (active banners)
- Click banner → navigate tới URL target

**Flash Sale section:**
- Countdown timer (giờ:phút:giây) tới hết flash sale
- Load flash sale hiện tại từ `GET /campaigns?type=flash_sale&status=active&limit=1`
- Hiển thị 4-6 sản phẩm flash sale dạng horizontal scroll, mỗi sản phẩm có: ảnh, tên, giá gốc gạch ngang, giá sale, % giảm, progress bar % đã bán

**Danh mục nổi bật:**
- Grid 4 cột × 2 hàng (8 danh mục) với ảnh + tên
- Load từ `GET /categories?featured=true&limit=8`

**Section "Gợi ý cho bạn":**
- Chỉ hiện khi đã đăng nhập
- Load từ `GET /ai/recommendations/homepage`
- Grid 4 cột, có nút "Xem thêm"

**Section "Sản phẩm bán chạy":**
- Grid 5 sản phẩm từ `GET /products?sort=soldCount&limit=10`
- Mỗi sản phẩm: ảnh, tên, giá, rating, số đánh giá

---

### Task 2 — ProductsPage + CategoryPage buyer-web

**ProductsPage** (hiện 63 dòng → cần ~350 dòng):
- **Filter sidebar bên trái:**
  - Khoảng giá: preset buttons (Dưới 100K, 100K-500K, 500K-1M, Trên 1M) + custom từ/đến
  - Rating tối thiểu: 5 radio buttons (1★ trở lên → 5★)
  - Danh mục: multi-checkbox với count, load từ `GET /categories?limit=20`
  - Free shipping toggle
- **Grid sản phẩm:**
  - Toggle grid/list view (2 chế độ hiển thị)
  - Sort bar: Liên quan | Bán chạy | Giá tăng | Giá giảm | Mới nhất | Đánh giá cao
  - Pagination (15 sản phẩm/trang)
  - URL sync: `?q=&category=&minPrice=&maxPrice=&rating=&sort=`
- **Active filter chips:** hiện bên trên grid, có nút × xóa từng filter

**CategoryPage** (hiện 96 dòng → cần ~280 dòng):
- Breadcrumb: Trang chủ > Category cha > Category hiện tại
- Subcategories: tabs hoặc chips dẫn sang subcategory
- Grid sản phẩm giống ProductsPage nhưng đã filter theo category
- Tiêu đề category + ảnh cover (nếu có)

**Backend cần thêm (catalog.routes.ts):**
- `GET /products` — đảm bảo support params: `category`, `minPrice`, `maxPrice`, `rating`, `freeShipping`, `sort`, `page`, `limit`
- `GET /categories?featured=true` — thêm filter featured

---

### Task 3 — AccountPage buyer-web nâng cấp

Trang hiện tại 60 dòng, chỉ hiện thông tin tĩnh. Làm thật:

**Tab navigation** (4 tabs):
1. **Hồ sơ** — form edit:
   - Avatar: click để upload (dùng `POST /upload/image`), preview ngay
   - Họ tên, email (readonly), số điện thoại, ngày sinh (date picker), giới tính (radio: Nam/Nữ/Khác)
   - Nút "Lưu thay đổi" → `PATCH /users/profile`
2. **Bảo mật** — đổi mật khẩu:
   - 3 field: Mật khẩu hiện tại, Mật khẩu mới, Xác nhận mật khẩu mới
   - Validate: mật khẩu mới ≥ 8 ký tự, trùng khớp
   - Submit → `PATCH /users/change-password`
3. **Địa chỉ** — inline address list:
   - Load từ `GET /users/addresses`
   - Nút thêm địa chỉ mới (modal: tên, SĐT, địa chỉ đầy đủ, tỉnh/thành, quận/huyện, mặc định)
   - Edit/Delete từng địa chỉ
   - Đánh dấu địa chỉ mặc định
4. **Đơn hàng gần đây** — hiện 5 đơn gần nhất với link tới OrderDetailPage

**Backend cần thêm (user.routes.ts):**
- `PATCH /users/profile` — update name, phone, birthday, gender, avatar
- `PATCH /users/change-password` — verify current password, set new
- `GET /users/addresses`, `POST /users/addresses`, `PATCH /users/addresses/:id`, `DELETE /users/addresses/:id`

---

### Task 4 — Admin DashboardPage + OrdersPage nâng cấp

**DashboardPage** (hiện 79 dòng → cần ~350 dòng):
- **KPI cards** (4 cards, gọi `GET /admin/analytics/summary`):
  - Tổng người dùng (+ growth % so tháng trước)
  - Tổng doanh thu tháng này
  - Đơn hàng hôm nay
  - Seller đang hoạt động
- **Revenue AreaChart:** recharts AreaChart 30 ngày từ `GET /admin/analytics/orders?period=30d`
- **Recent orders table:** 10 đơn gần nhất với: mã đơn, người mua, tổng tiền, trạng thái, thời gian
- **Recent registrations:** 5 user mới nhất với avatar, tên, thời gian đăng ký
- **Quick actions:** nút tắt "Duyệt seller" → link tới SellersPage?status=pending

**OrdersPage** (hiện 79 dòng → cần ~400 dòng):
- Filter: date range picker, status (multi-select), phương thức thanh toán (select)
- Search theo mã đơn hoặc email người mua
- Table columns: mã đơn, người mua, seller, tổng tiền, trạng thái, phương thức TT, ngày tạo
- Click row → OrderDetailPage (đã có)
- Export CSV nút trên header (gọi `GET /export/orders`)
- Pagination 20 đơn/trang

---

### Task 5 — Admin SellersPage + UsersPage nâng cấp

**SellersPage** (hiện 107 dòng → cần ~400 dòng):
- Filter: trạng thái (pending/active/suspended), tier (bronze/silver/gold), join date range
- Search theo tên shop hoặc email
- Table: avatar, tên shop, email, trạng thái, tier, GMV, số sản phẩm, join date
- **Bulk actions:** checkbox + "Duyệt hàng loạt" / "Tạm khóa hàng loạt"
- **Row actions:** Duyệt / Tạm khóa / Xem chi tiết
- Confirm dialog trước khi suspend
- Click "Xem chi tiết" → `/sellers/:id` (SellerDetailPage đã có)

**UsersPage** (hiện 100 dòng → cần ~350 dòng):
- Filter: role (buyer/seller/admin), trạng thái (active/banned), join date range
- Search theo tên hoặc email
- Table: avatar, tên, email, role, trạng thái, tổng đơn, join date
- **Row actions:** Xem profile, Ban/Unban (toggle với confirm), Reset password
- Bulk action: ban nhiều user cùng lúc

**Backend cần thêm (admin.routes.ts):**
- `PATCH /admin/sellers/bulk` — bulk approve/suspend
- `PATCH /admin/sellers/:id/status` — verify/suspend
- `PATCH /admin/users/:id/status` — ban/unban
- `PATCH /admin/users/:id/reset-password` — send reset email
- `GET /admin/sellers?status=&tier=&search=` — với filter params
- `GET /admin/users?role=&status=&search=` — với filter params

---

### Task 6 — Seller FinancePage + ReportsPage nâng cấp

**FinancePage** (hiện 106 dòng → cần ~400 dòng):
- **Balance overview cards:** Số dư khả dụng, Đang chờ duyệt, Tổng đã nhận tháng này
- **Transaction history table:**
  - Columns: ngày, loại (thu/chi), mô tả, số tiền (màu xanh/đỏ), số dư sau
  - Filter: loại giao dịch, date range
  - Pagination 20 giao dịch/trang
- **Payout request button:** mở modal yêu cầu rút tiền (số tiền, phương thức, tài khoản)
- Gọi `GET /finance/transactions`, `GET /finance/balance`, `POST /finance/payout-requests`

**ReportsPage seller** (hiện 72 dòng → cần ~350 dòng):
- **Revenue chart:** recharts BarChart theo tuần/tháng (toggle), 12 tuần hoặc 6 tháng
- **Order stats:** total/completed/cancelled/returned dạng pie chart (recharts PieChart)
- **Top 10 sản phẩm bán chạy:** table với ảnh nhỏ, tên, số lượng bán, doanh thu
- **Tỷ lệ đánh giá:** bar chart rating 1-5 sao
- Date range selector: tuần này / tháng này / 3 tháng / năm nay / custom
- Export báo cáo button

---

## NHÓM DỰ PHÒNG — Làm tiếp nếu còn token

### Task 7 — Admin ReturnsPage + WithdrawalsPage nâng cấp

**ReturnsPage** (hiện 72 dòng → cần ~350 dòng):
- Filter: trạng thái (pending/approved/rejected/refunded), date range
- Table: mã đơn, người mua, seller, lý do, trạng thái, số tiền hoàn, ngày tạo
- **Detail modal:** xem ảnh bằng chứng, mô tả lý do, timeline xử lý
- **Actions:** Duyệt (với nhập ghi chú), Từ chối (với lý do), Hoàn tiền ngay
- Bulk: approve nhiều return cùng lúc

**WithdrawalsPage** (hiện 105 dòng → cần ~300 dòng):
- Filter: trạng thái, date range, min/max amount
- Table: seller, số tiền, phương thức, tài khoản, trạng thái, ngày yêu cầu
- **Actions:** Duyệt (confirm dialog + nhập transaction ID), Từ chối (lý do)
- Bulk approve với confirm
- Tổng tiền cần giải quyết hôm nay (KPI card)

### Task 8 — Seller EditProductPage nâng cấp

Trang hiện tại 90 dòng, chỉ có form cơ bản. Làm thật:

- **Multi-image upload:**
  - Tối đa 8 ảnh, drag-drop để reorder
  - Click upload hoặc paste URL
  - Preview thumbnail, nút xóa từng ảnh
  - Ảnh đầu tiên = ảnh chính
- **Rich description:** textarea với basic markdown preview (toggle preview)
- **Variants matrix:**
  - Thêm attribute (VD: Màu: Đỏ, Xanh | Size: S, M, L)
  - Tự generate tổ hợp variant (Đỏ-S, Đỏ-M, Xanh-S,...)
  - Mỗi variant: SKU, giá, giá gốc, tồn kho
  - Bulk fill giá cho tất cả variants
- **Thông tin vận chuyển:** cân nặng (kg), kích thước (cm), thời gian xử lý (ngày)
- Preview button → mở tab mới xem trang sản phẩm (buyer-web)

### Task 9 — buyer-web ShopPage + CheckoutPage nâng cấp

**ShopPage** (hiện 169 dòng → cần ~350 dòng):
- **Shop header:** ảnh bìa full-width, avatar shop, tên shop, rating, follower count, số sản phẩm, năm thành lập
- **Follow/Unfollow button** (dùng API đã có)
- **Tab bar:** Tất cả | Sản phẩm | Đánh giá | Thông tin shop
- Tab Sản phẩm: search trong shop, filter category (category của shop), sort, grid
- Tab Đánh giá: list review của shop (aggregate reviews của tất cả sản phẩm)
- Tab Thông tin: mô tả shop, địa chỉ, giờ làm việc, chính sách hoàn trả

**CheckoutPage flow** (hiện 309 dòng — thêm features còn thiếu):
- **Address selector modal:** danh sách địa chỉ đã lưu với radio chọn, nút thêm địa chỉ mới
- **Coupon code input:** nhập mã → gọi `POST /promotions/apply-coupon` → hiện discount trực tiếp
- **Ghi chú đơn hàng:** textarea "Ghi chú cho seller"
- **Order summary:** cập nhật realtime khi apply coupon (subtotal, discount, shipping fee, total)

---

## Lưu ý khi làm

- Task 1-2: buyer-web catalog queries — kiểm tra catalog.service.ts có đủ filter params chưa, bổ sung nếu thiếu
- Task 3: cần thêm user endpoints (profile update, addresses CRUD) trong user.service.ts + user.routes.ts
- Task 4-5: admin endpoints — thêm vào admin.service.ts + admin.routes.ts
- Task 6: seller analytics endpoints — analytics.service.ts + analytics.routes.ts (một số đã có từ Task 4 ngày 14/6)
- Mỗi task commit 1 lần sau khi xong
- Cập nhật PROGRESS.md sau mỗi task
