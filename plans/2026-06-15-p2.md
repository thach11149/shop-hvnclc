# Kế hoạch 2026-06-15 — Bản 2

**Branch:** tạo từ `origin/claude/pensive-cerf-w35shf` (mới nhất, chứa tất cả code trước)
**Nguyên tắc:** làm thật, không placeholder. Xong nhóm chính → tự lấy nhóm dự phòng.

---

## Trạng thái đầu bản 2

Tất cả trang lớn đã nâng cấp xong. Còn lại là các trang chức năng cụ thể vẫn ở dạng skeleton mỏng (83-170 dòng). Ưu tiên: trang quan trọng cho user trực tiếp dùng hàng ngày.

---

## NHÓM CHÍNH — Bắt buộc hoàn thành

### Task 1 — buyer-web CampaignPage + NotificationsPage

**CampaignPage** (hiện 83 dòng → cần ~320 dòng):
- **Filter bar:** Tất cả | Voucher giảm giá | Free ship | Flash sale | Combo
- **Campaign card mỗi loại:**
  - Voucher: discount %, đơn tối thiểu, hết hạn, progress bar đã dùng/tổng
  - Flash sale: countdown timer (giờ:phút:giây), số sản phẩm, ảnh preview 3-4 sản phẩm
  - Freeship: điều kiện, vùng áp dụng
- **Nút "Lưu voucher"** (chỉ khi đã login) → `POST /campaigns/:id/claim` → badge "Đã lưu"
- Pagination (12 campaigns/trang)
- Gọi `GET /campaigns?type=&status=active&page=`

**NotificationsPage** (hiện 122 dòng → cần ~280 dòng):
- Tabs: Tất cả | Đơn hàng | Khuyến mãi | Hệ thống
- Mỗi notification: icon type, tiêu đề, mô tả, thời gian tương đối ("5 phút trước"), trạng thái đã đọc/chưa
- Unread notifications có nền nhạt + chấm xanh
- Nút "Đánh dấu tất cả đã đọc" → `PATCH /notifications/mark-all-read`
- Click vào notification → navigate tới trang liên quan (đơn hàng, sản phẩm,...)
- Delete notification (swipe hoặc nút ×)
- Pagination hoặc infinite scroll (10 items/page)
- Gọi `GET /notifications?type=&page=`, `PATCH /notifications/:id/read`

---

### Task 2 — buyer-web PaymentMethodsPage + ReviewsPage

**PaymentMethodsPage** (hiện 94 dòng → cần ~300 dòng):
- **Danh sách ví/thẻ đã liên kết:**
  - VNPay: số tài khoản (ẩn bớt), ngân hàng, trạng thái (verified/unverified)
  - MoMo: số điện thoại, trạng thái
  - ZaloPay: số điện thoại
  - Thẻ tín dụng (nếu có): số thẻ ẩn bớt, tên trên thẻ, hết hạn
- **Thêm phương thức thanh toán:**
  - Button "Liên kết VNPay/MoMo/ZaloPay" → modal hướng dẫn liên kết (step-by-step)
  - Nút "Đặt làm mặc định"
  - Nút "Xóa liên kết" + confirm dialog
- **Lịch sử giao dịch:** 5 giao dịch gần nhất mỗi ví (ngày, mô tả, số tiền)
- Gọi `GET /users/payment-methods`, `DELETE /users/payment-methods/:id`, `PATCH /users/payment-methods/:id/default`

**ReviewsPage** (hiện 118 dòng → cần ~280 dòng):
- Danh sách review buyer đã viết: ảnh sản phẩm, tên, rating sao, comment, ngày viết
- Badge "Có phản hồi của seller" nếu seller đã reply
- Nút **Edit review** (chỉ khi chưa có reply) → modal edit rating + comment
- Nút **Xóa review** + confirm (chỉ khi chưa có reply)
- Filter: Tất cả | Chưa có reply | Đã có reply | Theo sao (1★/2★...)
- Empty state: "Bạn chưa viết đánh giá nào" + CTA mua hàng
- Gọi `GET /reviews/my-reviews`, `PATCH /reviews/:id`, `DELETE /reviews/:id`

---

### Task 3 — seller-center StaffsPage + QnAManagementPage

**StaffsPage** (hiện 137 dòng → cần ~380 dòng):
- **Danh sách nhân viên:** avatar, tên, email, role badge (Quản trị/Kho/CSKH), trạng thái (active/pending), ngày thêm vào
- **Mời nhân viên mới:** modal — nhập email, chọn role, gửi lời mời → `POST /seller/staffs/invite`
- **Roles & permissions:**
  - Quản trị (Admin): toàn quyền
  - Kho (Warehouse): chỉ Kho hàng + Sản phẩm
  - CSKH (Customer Service): chỉ Đơn hàng + Chat + Q&A + Tranh chấp
- **Toggle active/inactive** per staff
- **Revoke access** + confirm dialog
- **Pending invites** section (email đã mời nhưng chưa accept)
- Gọi `GET /seller/staffs`, `POST /seller/staffs/invite`, `PATCH /seller/staffs/:id/status`, `DELETE /seller/staffs/:id`

**QnAManagementPage** (hiện 152 dòng → cần ~350 dòng):
- **Badge** số câu hỏi chưa trả lời trên tab title
- Filter tabs: Tất cả | Chưa trả lời | Đã trả lời | Đã ẩn
- Search theo nội dung câu hỏi hoặc tên sản phẩm
- Mỗi Q&A card:
  - Ảnh + tên sản phẩm (click → trang sản phẩm)
  - Câu hỏi của buyer + tên (ẩn bớt) + thời gian
  - **Form trả lời** nếu chưa có answer: textarea + submit → `POST /qna/:id/answer`
  - **Câu trả lời hiện tại** nếu đã có + nút Edit
  - Nút Ẩn câu hỏi (xóa mềm)
- Bulk actions: ẩn nhiều câu hỏi cùng lúc
- Pagination 10 items/trang

---

### Task 4 — seller-center FreeshippingPage + PayoutPage

**FreeshippingPage** (hiện 149 dòng → cần ~350 dòng):
- **Danh sách quy tắc freeship:**
  - Mỗi rule: đơn tối thiểu, vùng áp dụng (toàn quốc/miền Bắc/Trung/Nam), thời gian, trạng thái, toggle bật/tắt
- **Create/Edit rule modal:**
  - Tên rule, đơn tối thiểu (input số tiền)
  - Vùng áp dụng: Toàn quốc / Chọn tỉnh thành (multi-checkbox)
  - Thời gian: luôn luôn / khoảng thời gian (datetime range)
  - Giới hạn số lượt dùng/ngày (optional)
- **Freeship ngân sách:** thấy được chi phí freeship seller đang chi trả tháng này
- Gọi `GET /seller/freeship-rules`, `POST /seller/freeship-rules`, `PATCH /seller/freeship-rules/:id`, `DELETE /seller/freeship-rules/:id`

**PayoutPage** (hiện 155 dòng → cần ~350 dòng):
- **Balance cards:**
  - Số dư khả dụng (lớn, màu xanh)
  - Đang giữ (holding period)
  - Tổng đã nhận tháng này
- **Request payout modal:**
  - Số tiền muốn rút (max = số dư khả dụng, min = 100,000đ)
  - Chọn tài khoản ngân hàng (dropdown từ saved accounts) hoặc thêm tài khoản mới
  - Preview: số tiền sau phí (nếu có)
  - Confirm → `POST /finance/payout-requests`
- **Lịch sử payout:**
  - Table: ngày yêu cầu, số tiền, tài khoản, trạng thái (pending/processing/completed/rejected), ngày xử lý
  - Filter: trạng thái, date range
  - Status badge color-coded
- **Tài khoản ngân hàng đã lưu:** list + add new (tên NH, số TK, tên chủ TK)
- Gọi `GET /finance/payout-requests`, `POST /finance/payout-requests`, `GET /finance/bank-accounts`, `POST /finance/bank-accounts`

---

### Task 5 — seller-center ReviewManagementPage + InventoryAlertPage

**ReviewManagementPage** (hiện 170 dòng → cần ~370 dòng):
- **Rating overview:**
  - Score trung bình lớn (VD: 4.7 ★)
  - Bar chart 5-1 sao với % từng mức
  - Tổng số đánh giá
- **Filter:** theo sao (1★-5★), đã reply/chưa reply, date range, sản phẩm (search)
- **Review card:**
  - Avatar + tên buyer (ẩn bớt) + sao + ảnh đánh kèm (gallery) + comment
  - Tên sản phẩm + variant
  - **Reply form** (nếu chưa có reply): textarea + submit → `POST /reviews/:id/reply`
  - **Reply đã có**: hiện phản hồi + nút Edit reply
  - Nút Flag (báo cáo review không phù hợp)
- Pagination 10 items

**InventoryAlertPage** (hiện 156 dòng → cần ~320 dòng):
- **KPI cards:** Tổng SKU, Sắp hết (< ngưỡng), Đã hết (= 0)
- **Alert list:**
  - Sort: hết hàng trước → sắp hết → đủ hàng
  - Mỗi item: ảnh, tên, SKU, tồn kho hiện tại, ngưỡng cảnh báo, progress bar mức độ
  - Color: đỏ (= 0), cam (< ngưỡng), xanh (đủ)
  - Nút "Đặt hàng nhập" → link tới WarehouseInboundPage
  - Inline edit ngưỡng cảnh báo (click vào số → input)
- **Cài ngưỡng mặc định** cho tất cả sản phẩm (bulk set)
- Gọi `GET /warehouse/inventory?sort=stock_asc`, `PATCH /warehouse/inventory/:productId/threshold`

---

### Task 6 — admin-console EmailTemplatesPage + PaymentConfigPage + OperationsReportPage

**EmailTemplatesPage** (hiện 174 dòng → cần ~380 dòng):
- **Danh sách templates:**
  - welcome, order-confirmed, order-shipped, order-delivered, dispute-update, payout-processed, seller-approved
  - Mỗi template: tên, subject, last modified, preview button
- **Editor modal:**
  - Tab "Nội dung": textarea HTML với basic syntax highlight (hoặc plain text)
  - Tab "Preview": render HTML preview trong iframe/div
  - Edit subject (input)
  - Variables helper: danh sách biến có thể dùng `{{buyer_name}}`, `{{order_id}}`,...
  - Nút **Gửi test** → modal nhập email → `POST /system-config/emails/:name/send-test`
  - Save → `PATCH /announcement/:id` hoặc `PATCH /system-config/email-templates/:name`

**PaymentConfigPage** (hiện 172 dòng → cần ~350 dòng):
- **Danh sách payment methods:**
  - VNPay, MoMo, ZaloPay, COD, Chuyển khoản ngân hàng
  - Toggle bật/tắt từng method
  - Fee config: % platform fee per method
- **VNPay config section:**
  - TMN Code, Hash Secret (hiện dạng password *** với nút reveal)
  - Return URL (readonly, auto-generated)
  - Test connection button → `POST /payment/test-connection?gateway=vnpay`
- **MoMo config:** tương tự
- **ZaloPay config:** tương tự
- **COD settings:** có thu phí COD không, phí bao nhiêu %
- Save all → `PATCH /system-config/payment`

**OperationsReportPage** (hiện 162 dòng → cần ~370 dòng):
- **SLA metrics cards:**
  - Thời gian xử lý đơn TB (từ placed → confirmed): X giờ
  - Thời gian giao hàng TB: X ngày
  - Thời gian giải quyết tranh chấp TB: X ngày
  - Tỷ lệ đơn giao đúng hẹn: X%
- **Fulfillment rate chart:** recharts AreaChart theo tuần/tháng
- **On-time delivery by carrier:** bảng so sánh các đơn vị vận chuyển
- **Return rate by category:** bar chart top 10 category có tỷ lệ hoàn cao nhất
- **Seller performance:** table (tên shop, tỷ lệ xác nhận đúng hẹn, rating TB, số khiếu nại)
- Date range filter: 7 ngày / 30 ngày / 3 tháng
- Gọi `GET /admin/analytics/operations?period=30d`

---

## NHÓM DỰ PHÒNG — Làm tiếp nếu còn token

### Task 7 — seller-center ShopDecorationPage nâng cấp

Trang hiện tại 169 dòng. Làm thật:
- **Sections**: Banner slider (upload ảnh, thêm tối đa 5 ảnh, reorder), Featured Products (chọn 4-8 sản phẩm nổi bật), Announcement bar (text, màu nền, bật/tắt)
- **Theme settings**: màu chủ đạo (color picker), font size (S/M/L)
- **Live preview panel**: xem shop trông như thế nào (iframe hoặc div mock)
- Save → `PATCH /sellers/decoration`

### Task 8 — buyer-web AIShoppingAssistantPage + TrackingPage nâng cấp

**AIShoppingAssistantPage** (hiện 120 dòng):
- Chat interface: input ở dưới, messages scroll ở trên
- Suggested questions: "Tìm áo thun dưới 200K", "So sánh iPhone 15 và Samsung S24"
- AI response có thể kèm product cards (tên, ảnh, giá, link)
- Typing indicator khi AI đang xử lý
- Gọi `POST /ai/assistant` `{message, conversationId?}` → `{reply, products?[]}`

**TrackingPage** (hiện 140 dòng):
- Input tracking number + chọn carrier (dropdown)
- Gọi `GET /tracking?trackingNumber=&carrier=` → tracking info
- Hiển thị: timeline tracking steps (đã lấy hàng → đang vận chuyển → đến kho trung tâm → đang giao → đã giao)
- Mỗi step: địa điểm, timestamp, mô tả
- Delivery estimate badge

### Task 9 — admin-console AdsManagementPage + WarehousesPage nâng cấp

**AdsManagementPage** (hiện 170 dòng):
- Filter: seller (search), loại (CPC/CPM), trạng thái (active/paused/ended), date range
- Table: seller, tên campaign, loại, chi tiêu, impressions, clicks, CTR, trạng thái, actions
- **Actions admin:** Duyệt (nếu pending), Tạm dừng, Xem chi tiết
- Daily spend AreaChart (tổng tất cả sellers)
- Export CSV

**WarehousesPage** (hiện 114 dòng):
- List kho hàng: tên, địa chỉ, quản lý (admin), sức chứa %, số SKU đang lưu, trạng thái
- Create/Edit modal: tên, địa chỉ, quản lý phụ trách, sức chứa tối đa (m3 hoặc số pallet)
- Toggle active/inactive

---

## Lưu ý

- Branch từ `origin/claude/pensive-cerf-w35shf`
- Không cần sửa `App.tsx` hay `app.ts` — tất cả routes đã có
- Commit sau mỗi task, cập nhật `PROGRESS.md`
